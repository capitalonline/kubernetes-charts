# ==========================================
# Component Configuration
# ==========================================
loki:
  retention: 30d
  maxQueryLength: 30d
  storage:
    storageClassName: default-local-sc
    size: 20Gi

# ==========================================
# Container Configuration (Unified Namespace)
# ==========================================
container:
  # Loki main container
  loki:
    image:
      repository: capitalonline/loki
      tag: "3.2.0"
  
  # Promtail log collection
  promtail-container:
    image:
      repository: capitalonline/promtail
      tag: "3.2.0"
  
  # Kubernetes event exporter
  event-exporter:
    image:
      repository: capitalonline/kubernetes-event-exporter
      tag: v1.7

