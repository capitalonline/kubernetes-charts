# ==========================================
# Component Configuration
# ==========================================
# (No component-level configuration for this component)

# ==========================================
# Container Configuration (Unified Namespace)
# ==========================================
container:
  # DCGM Exporter main container (NVIDIA GPU monitoring)
  dcgm-exporter:
    image:
      repository: registry-cds.yun-paas.com/cds-eks/dcgm-exporter
      tag: 3.3.8-3.6.0-ubuntu22.04-v3
  
  # Kube RBAC Proxy (sidecar)
  kube-rbac-proxy:
    image:
      repository: registry-cds.yun-paas.com/cds-eks/kube-rbac-proxy
      tag: v0.14.2

